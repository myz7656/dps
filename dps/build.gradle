apply plugin: 'groovy'
apply plugin: 'maven'

apply from: 'gradle_mvn_push.gradle'

repositories {
    mavenLocal()
    jcenter()
    maven { url 'https://repo.gradle.org/gradle/libs-releases' }
}

dependencies {
    compile gradleApi()
}